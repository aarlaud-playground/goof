resources:
- name: goof
  type: git
  source:
    uri: https://github.com/aarlaud-playground/goof
    branch: master

jobs:
- name: Install Dependencies
  plan:
  - get: goof
    trigger: true
  - task: build app
    file: goof/build.yml
